# Copy to /etc/modelbytes/postgres.env on the VM and chmod 600.
# POSTGRES_PASSWORD must match the password embedded in modelbytes.env DATABASE_URL.

POSTGRES_DB=modelbytes
POSTGRES_USER=modelbytes
POSTGRES_PASSWORD=replace-me
